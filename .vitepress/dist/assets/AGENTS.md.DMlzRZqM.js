import{H as e,ht as t,nt as n,tt as r}from"./chunks/framework.DPaNRfRs.js";var i=JSON.parse(`{"title":"AGENTS.md","description":"","frontmatter":{},"headers":[],"relativePath":"AGENTS.md","filePath":"AGENTS.md"}`),a={name:`AGENTS.md`};function o(e,i,a,o,s,c){return t(),r(`div`,null,[...i[0]||=[n(`<h1 id="agents-md" tabindex="-1">AGENTS.md <a class="header-anchor" href="#agents-md" aria-label="Permalink to “AGENTS.md”">​</a></h1><p>面向 AI 代理的项目工作约定。修改本项目前请先阅读本文。</p><h2 id="项目概览" tabindex="-1">项目概览 <a class="header-anchor" href="#项目概览" aria-label="Permalink to “项目概览”">​</a></h2><ul><li>技术栈：VitePress 2.0（alpha）+ Vue 3，纯静态文档/博客站点。</li><li>内容：<code>index.md</code>（首页）+ <code>docs/</code>（文档页）。</li><li>UI：VitePress 默认主题（<code>DefaultTheme</code>）+ <code>@yunyoujun/ak-ui</code>（明日方舟风格 UI 组件库）。</li><li>语言：站内内容与交流统一使用中文（zh-CN）。</li></ul><h2 id="常用命令" tabindex="-1">常用命令 <a class="header-anchor" href="#常用命令" aria-label="Permalink to “常用命令”">​</a></h2><table tabindex="0"><thead><tr><th>命令</th><th>用途</th></tr></thead><tbody><tr><td><code>npm run docs:dev</code></td><td>启动开发服务器（默认端口 5173）</td></tr><tr><td><code>npm run docs:build</code></td><td>构建到 <code>.vitepress/dist</code></td></tr><tr><td><code>npm run docs:preview</code></td><td>预览构建产物</td></tr></tbody></table><h2 id="项目结构" tabindex="-1">项目结构 <a class="header-anchor" href="#项目结构" aria-label="Permalink to “项目结构”">​</a></h2><div class="language-"><button title="Copy code" data-copied="Copied" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="--shiki-light:#24292e;--shiki-dark:#e1e4e8;--shiki-light-bg:#fff;--shiki-dark-bg:#24292e;" tabindex="0" dir="ltr"><code><span class="line"><span>.vitepress/</span></span>
<span class="line"><span>  config.js        # 站点配置（唯一生效的配置入口）</span></span>
<span class="line"><span>  theme/</span></span>
<span class="line"><span>    index.js       # 主题入口（唯一生效的入口，extends DefaultTheme）</span></span>
<span class="line"><span>    style.css      # 全局样式与 ak-ui 组件替换（保留双份规则）</span></span>
<span class="line"><span>index.md           # 首页（hero + features）</span></span>
<span class="line"><span>docs/              # 文档页</span></span>
<span class="line"><span>package.json</span></span></code></pre></div><h2 id="关键约定-务必遵守" tabindex="-1">关键约定（务必遵守） <a class="header-anchor" href="#关键约定-务必遵守" aria-label="Permalink to “关键约定（务必遵守）”">​</a></h2><ol><li><strong>主题入口只允许 <code>theme/index.js</code></strong>。不要新增 <code>index.ts</code>：Vite 按 <code>.js</code> → <code>.ts</code> 顺序解析，<code>.ts</code> 会被 <code>.js</code> 遮蔽而永远不生效。</li><li><strong>配置入口只允许 <code>config.js</code></strong>。不要新增 <code>config.mts</code>，同样会被遮蔽。</li><li><strong>ak-ui 样式&quot;保留双份&quot;规则</strong>：当需要用 ak-ui 类名替换原有样式名时，复制一份 <code>ak-*</code> 规则改名为目标名（如 <code>.ak-button</code> → <code>.button</code>），<strong>不要直接修改原 <code>.ak-*</code> 规则</strong>，保证两套类名同时可用。所有替换集中在 <code>theme/style.css</code> 的&quot;ak-ui 组件替换&quot;区块。</li><li><strong>覆盖 VitePress 内置 scoped 样式必须加 <code>!important</code></strong>（如 <code>.VPButton</code>、<code>.VPFeature</code>、<code>.VPHero .name</code>）。</li><li><strong>不要修改 <code>node_modules</code></strong>。对 ak-ui 的定制一律通过 <code>style.css</code> 覆盖实现（CI 重装依赖后 node_modules 会还原）。</li><li>首页 Hero 标题宽度覆盖位于 <code>style.css</code> 中 <code>.VPHero .name</code> 的 <code>max-width</code> 规则。</li><li>提交信息遵循 Conventional Commits（见 <code>.trae/rules/git-commit-message.md</code>）：<code>&lt;type&gt;(&lt;scope&gt;): &lt;subject&gt;</code>，type 小写英文、subject 中文，一次提交一个核心改动。</li><li>构建产物与缓存（<code>.vitepress/dist</code>、<code>.vitepress/cache</code>、<code>.vitepress/.temp</code>）已被 <code>.gitignore</code> 忽略，不要提交。</li><li>网络资源（如 ak-ui CDN CSS、Google 字体 Noto Sans/Serif SC）通过 <code>config.js</code> 的 <code>head</code> 配置注入。</li><li>每次完成任务后检查是否需要更新 <code>AGENTS.md</code>。</li><li><strong>全站主题变量映射集中在 <code>style.css</code> 顶部</strong>：<code>--ak-*</code> 调色板/字体变量 → <code>--vp-c-*</code>（明/暗双主题）、<code>--vp-button-*</code>、<code>--vp-home-hero-*</code>、<code>--vp-custom-block-*</code>。新增全站风格化时优先改变量映射，避免硬编码颜色；文档风格化（导航/侧边栏/代码块/表格/引用/滚动条）位于 style.css 的&quot;组件细节&quot;区块。</li></ol>`,10)]])}var s=e(a,[[`render`,o]]);export{i as __pageData,s as default};